import { TipoPropostaPipe } from './tipo-proposta.pipe';

describe('TipoPropostaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPropostaPipe();
    expect(pipe).toBeTruthy();
  });
});
